# python program to learn git from command line, no github
def hello(msg):
    # works only on Python 3.7+
    print(f"Hello {msg}!")

if __name__ == "__main__":
    hello("world")
    hello("Alice")
    hello("Bob")
